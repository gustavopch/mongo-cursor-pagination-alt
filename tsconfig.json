{
    "extends": "./node_modules/@newforma/presets-lambda/configs/typescript/base.json",
    "include": ["**/*"],
    "exclude": ["buildOutput", "coverage", "deploy", "node_modules"],
    "compilerOptions": {
        "outDir": "buildOutput"
    }
}
