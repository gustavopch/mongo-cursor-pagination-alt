#!/usr/bin/env groovy
@Library('pipe3') _

def config = [:]
config.serviceName = 'mongo-cursor-pagination-alt'
config.slackChannel = '#waldo-build'
config.languageVersion = 'node-12'
config.infrastructure = 'npm'

npmPkg(config)
